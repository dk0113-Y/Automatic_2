# Automatic_2

`Automatic_2` is the single control-plane tuning repository for future DRL training analysis.

First document to read:

- `docs/training_system_manifest.md`

The real training implementation, outputs, logs, checkpoints, and model weights remain outside this repository in the `DRL-path-finding` training repository. This repository stores control-plane summaries and future sanitized reports only.
